# Contributing

Keep claims tied to committed raw evidence. Run `make format lint test build
security` before opening a change. New correlation rules need a documented score,
counter-evidence behavior, a focused test, and an ADR when they change the model.
Never add request, trace, customer, or order identity as a metric label.

